/**
 * Created by marno on 2017/4/10
 * Function: 所有自定义组件类的入口类（此类勿删）
 * Desc: 名字默认为 index.js ，修改后会报错。
 *       所有组件必须通过这个类导出一遍才能在其他类中调用
 */
import ImageButton from './ImageButton'
import TitleBar from './TitleBar';

export {ImageButton, TitleBar}
